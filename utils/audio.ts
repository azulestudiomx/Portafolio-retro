
// Synthesized Retro Sounds using Web Audio API
// No external assets needed!

// Sounds are disabled per user request.

export const playClickSound = () => {
  // Disabled
};

export const playStartupSound = () => {
  // Disabled
};

export const playWindowDragSound = () => {
  // Disabled
}
